# Prometheus — minimal local config for docker-compose.observe.yml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: otelcol
    static_configs:
      - targets: ["otelcol:8888"]  # collector's own metrics
